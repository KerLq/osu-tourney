<%= form_with(model: match) do |form| %>
  <% if match.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(match.errors.count, "error") %> prohibited this match from being saved:</h2>

      <ul>
        <% match.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :mp_link %>
    <%= form.text_field :mp_link %>
  </div>

  <div class="field">
    <%= form.label :warmup %>
    <%= form.text_field :warmup %>
  </div>

  <div class="field">
    <%= form.label :matchcost %>
    <%= form.text_field :matchcost %>
  </div>

  <div class="field">
    <%= form.label :average_score %>
    <%= form.text_field :average_score %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
