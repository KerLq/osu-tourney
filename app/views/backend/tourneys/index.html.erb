<p id="notice"><%= notice %></p>

<h1>Tourneys</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Spreadsheet</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tourneys.each do |tourney| %>
      <tr>
        <td><%= tourney.title %></td>
        <td><%= tourney.spreadsheet %></td>
        <td><%= link_to 'Show', backend_user_tourney_path(@user, tourney) %></td>
        <td><%= link_to 'Edit', edit_backend_user_tourney_path(@user, tourney) %></td>
        <td><%= link_to 'Destroy', backend_user_tourney_path(@user, tourney), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
