<%= form_for [:frontend, @user, @tourney], remote: true, html: { class: 'popup-form' } do |f| %>
  <%= f.label :forumpost %>
  <%= f.text_field :forumpost %>
  <%= f.button "Submit", class: "button popup-form__button" %>
<% end %>
